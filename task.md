# AgentVillage 작업 기록 (task.md)

> 포켓몬 스타일 탑뷰 2D RPG — AI 에이전트 마을.
> 플레이어가 마을/구역을 돌아다니며 건물의 AI 에이전트(Claude)에게 작업을 의뢰하는 로컬 웹게임.
> 이 문서는 **요구사항과 작업 진행을 시간순으로 기록**하며, 새 지시가 들어와 관련 작업을 하면 자동으로 최신화한다.

- 프로젝트 경로: `/Users/dohee/Documents/workspace/project/AgentVillage`
- 참고 프롬프트: `/Users/dohee/Downloads/agent-village-prompts`
- 실행 포트: **47913** (`http://localhost:47913`)
- 최초 작성일: 2026-06-02

---

## 기술 스택
- 런타임: Node.js + Express (로컬 서버)
- 프론트엔드: 순수 HTML5 Canvas + Vanilla JS (프레임워크/TS 없음, 인라인 CSS만)
- AI: **Claude CLI 서브프로세스(`claude -p --output-format stream-json`)** — 로그인된 구독 인증 사용, API 키 불필요(추가비용 없음). `.env`의 `AI_MODE=api`로 전환 시 `@anthropic-ai/sdk` 사용 가능.
- 실시간: WebSocket (`ws`)
- 영구 저장: 커스텀 에이전트 → `custom-agents.json`

## 파일 구조
```
AgentVillage/
├── server.js        Express + WebSocket 서버, AI 작업 중계, 커스텀 에이전트 CRUD
├── agents.js        빌트인 7개 에이전트 + 오케스트레이터 멀티에이전트 + 커스텀 레지스트리(영구 저장)
├── package.json / .env / .env.example / .gitignore
├── custom-agents.json   (런타임 생성, gitignore)
├── task.md          (이 문서)
└── public/
    ├── index.html   Canvas 마운트, 스크립트 로드
    ├── sprites.js   픽셀아트 그리기 + AGENT_META
    ├── world.js     다중 구역(마을/공방) 타일맵·충돌·건물·게이트
    ├── player.js    이동·충돌·카메라·걷기 애니메이션
    ├── buildings.js 건물 입장 감지
    ├── interior.js  실내 씬·NPC 배치·공방(마스터+커스텀)
    ├── ui.js        대화창·타이핑·작업 입력
    ├── editor.js    에이전트 공방 에디터(목록/추가/편집/삭제)
    └── game.js      메인 루프·씬 관리·입력·WebSocket·전환·미니맵
```

---

## 요구사항 & 작업 이력 (시간순)

### 1. 프로젝트 생성 및 전체 게임 구현 — ✅ 완료
- **요구**: `project/` 아래 `AgentVillage` 생성, 참고 프롬프트대로 개발.
- **결정**: AI 연동은 **API 키 대신 Claude CLI 구독 인증**(추가비용 없음) 방식 채택.
- **작업**: 명세 10단계대로 server.js, agents.js, public/* 전부 구현.
- **검증**: 단일 에이전트(coder) + 오케스트레이터 멀티에이전트(coder→writer 분해) WS 스트리밍 실동작 확인.

### 2. 실행 포트 변경 — ✅ 완료
- **요구**: 기존 프로젝트와 겹치지 않는 비표준 포트.
- **작업**: `.env` PORT `3000 → 47913`. 포트 충돌 안내 메시지 추가(server.js).

### 3. 조작/입장 개선 1차 — ✅ 완료
- **요구**: ① 대각선 이동 금지 ② 캐릭터 약간 확대 ③ 각 집에 이름 표시 ④ 문에 닿으면 자동 입장 ⑤ 상호작용 키 E→Enter.
- **작업**:
  - player.js: 최근 누른 방향 1개만 적용(단일 축), 발 기준 1.2배 확대.
  - world.js: 건물 이름 라벨 상시 표시.
  - game.js: 문 타일 자동 입장(+퇴장 직후 재입장 방지), 상호작용 Enter로 변경.

### 4. 퇴장/화면 전환 — ✅ 완료
- **요구**: ① 출구에 닿으면 자동 퇴장 ② 포켓몬식 검은 박스가 휘감으며 채워지는 화면 전환(로딩 느낌).
- **작업**: game.js에 출구 매트 자동 퇴장 + **시계방향 나선 블록 와이프** 전환(덮기→씬교체→안쪽부터 풀림), 전환 중 입력 잠금. 나선은 방문 시뮬레이션으로 각 셀 정확히 1회.

### 5. UI/연출 개선 — ✅ 완료
- **요구**: ① 좌우 이동 시 다리 방향 교차(사실적) ② 한글 선명하게 ③ 좌상단 WASD HUD 제거 ④ 우하단 미니맵 ⑤ 마을 외 다른 구역 + 에이전트 커스텀 공간.
- **작업**:
  - sprites.js: 좌/우 이동 시 앞·뒤 다리 진행방향 교차(뒷다리 어둡게), 상/하는 양다리 스트라이드.
  - 한글 선명: 캔버스를 기기 해상도(devicePixelRatio)로 렌더 + 한글 친화 시스템 폰트, 크기 확대.
  - ui.js: 좌상단 HUD 힌트 제거.
  - game.js: 우하단 미니맵(건물·플레이어·시야 표시).
  - **새 구역 + 커스텀(아래 6번에서 상세)**.

### 6. 다른 구역 + 에이전트 커스텀 공방 — ✅ 완료
- **요구**: 마을 외 다른 구역, 그 구역에서 NPC에게 말 걸어 에이전트 개수/기능 커스텀.
- **결정(사용자 확인)**: 편집 범위=개수+이름+역할+색 전부 / 커스텀 에이전트는 그 구역에 NPC로 등장해 바로 대화·작업 / **파일로 영구 저장**.
- **작업**:
  - agents.js: 커스텀 에이전트 레지스트리 + `custom-agents.json` 영구 저장(add/update/delete/list).
  - server.js: WS 메시지 `list_agents·create_agent·update_agent·delete_agent`.
  - world.js: 다중 구역(마을↔공방), 남쪽 **게이트**로 나선 전환 이동(양방향). 문-땅 정렬 위해 문→광장 L자 도로 연결.
  - interior.js: 공방 실내 = **마스터 NPC**(편집 ✎) + 커스텀 에이전트 NPC 격자.
  - editor.js(신규): 공방 에디터 — 목록/추가/편집/삭제(한글 IME 입력, 색 팔레트 선택).
  - game.js: 게이트 이동, 마스터=에디터 열기, 커스텀 에이전트=대화/작업, 목록 캐시·동기화.
- **검증**: WS CRUD + 커스텀 에이전트로 실제 작업 실행(번역) 성공, 파일 저장 확인. 구역 이동·공방 입장·에디터 렌더 런타임 검증.

### 7. 집 입구-땅 불일치 수정 — ✅ 완료
- **요구**: 집 입구(문)와 땅(길)이 어긋나는 곳 수정.
- **작업**: world.js에서 각 건물 문을 광장과 **L자 도로로 확실히 연결**(`connectToPlaza`)하여 외딴 path 타일 제거.

### 8. task.md 작성 및 지속 최신화 — ✅ 진행 중(상시)
- **요구**: 지금까지의 요구사항/작업을 시간순으로 정리한 `task.md` 생성, 이후 지시 관련 작업 시 자동 최신화.
- **작업**: 본 문서 생성. 이후 모든 작업 항목을 이 이력에 추가/갱신.

### 9. 화면 확대·미니맵·지형지물·실내 장식 — ✅ 완료
- **요구**: ① 화면을 브라우저 가득 채우기 ② 미니맵의 "MAP" 글자 제거 ③ 주변 지형지물/기능 추가 ④ 건물 내부 장식품(소파·의자 등) 추가.
- **작업**:
  - game.js: **반응형 캔버스**(`resizeCanvas`) — 창 크기에 맞춰 비율 유지 확대 + `resize` 대응, 고해상도 유지. 미니맵 "MAP" 텍스트 제거.
  - index.html: 캔버스가 뷰포트를 채우도록 CSS 변경, 제목/도움말은 오버레이로.
  - sprites.js: 야외(바위·덤불·가로등·우물·표지판) + 실내 가구(러그·소파·의자·테이블·책장·화분) 스프라이트 추가.
  - world.js: 두 구역에 장식 지형지물 배치 + **상호작용 게시판/표지판**(Enter로 안내 표시, `signAt`).
  - interior.js: 실내에 가구 배치(장식, y정렬, 중앙 통로는 비움).
  - game.js: 게시판/표지판 Enter 상호작용(`openInfo`), 대화 상태 처리 정리(handleInteract/handleBack).
- **검증**: 문법 + DOM 런타임(반응형 크기/리사이즈, 지형지물·미니맵 렌더, 게시판 Enter→안내→ESC, 실내 가구 렌더) 전부 통과.

### 10. 문 위치-입장 좌표 불일치 버그 픽스 + 시각 개선 (ralph) — ✅ 완료
- **요구**: 보이는 집 문 위치와 실제 입장 가능 좌표가 다름 → Playwright로 픽스될 때까지 ralph 루프. 이어서 집/길/지형지물 시각 개선.
- **원인**: drawBuilding이 문을 건물 기하중심(`sx+floor(w/2)`)에 그렸으나, placeBuilding 트리거 타일은 `tx+floor(w/2)` 중심 → 짝수폭(4·8) 건물에서 8px(반 타일) 어긋남(홀수폭 5는 우연히 일치).
- **수정(public/sprites.js)**:
  - drawBuilding 문을 트리거 타일 중심에 정렬: `doorCenterX = sx + floor(wTiles/2)*TILE + TILE/2`.
  - 시각 개선: 지붕 슁글 줄무늬+용마루 하이라이트, 문틀+발판, 창틀(십자살, drawWindow), 벽 베이스보드/모서리 음영, 굴뚝 / path 도로 텍스처·자갈, grass 풀결, water 잔물결, 나무 하이라이트.
- **검증**: 실제 drawBuilding 계측으로 6개 건물 문 중심=트리거 중심 diff=0(시각 개선 후에도 유지). DOM 회귀(입장/퇴장/구역이동) OK. Playwright 실브라우저 로드(JS 에러 0)+전체화면 스크린샷으로 시각 확인. architect 리뷰 APPROVED. (참고: 이 환경 playwright MCP는 browser_evaluate가 1초 타임아웃으로 navigate/screenshot만 가능 → 픽셀 정렬은 실제 코드 계측으로 결정적 증명.)

### 11. 다양한 테마 맵 추가 (숲·해변·설원·동굴) — ✅ 완료
- **요구**: 여러 다양한 맵 추가.
- **작업**:
  - **마을을 허브로**: 동(숲)·서(해변)·북(설원) 게이트 + 연결 도로 추가(기존 남=공방).
  - **신규 4개 구역**(world.js): 숲(흙길·나무·침엽수·버섯), 해변(모래·바다·야자수·조개·등대), 설원(눈·얼음호수·침엽수·눈사람), 동굴(어두운 바닥·크리스탈·석순·바위). 숲↔동굴 연결(숲 북쪽 게이트), 각 구역 남쪽 게이트로 마을 복귀.
  - **신규 타일**(sprites.js): dirt, snow, ice, deep_water, cave_floor.
  - **신규 지형지물**(sprites.js): 버섯·야자수·눈사람·침엽수·크리스탈·석순·조개·등대.
  - 렌더링: 평면(버섯·조개) + y정렬(야자수·눈사람·침엽수·크리스탈·석순·등대) 분기 추가. 경로 페인트에 타일 지정·비충돌 prop 옵션 추가, fillGround 헬퍼.
- **검증**: node --check OK. DOM 런타임으로 6개 구역 빌드+렌더 예외 없음, **전체 게이트 이동 경로**(마을↔숲↔동굴, 마을↔해변, 마을↔설원) 정상. Playwright 실브라우저 로드 JS 에러 0 + 마을 허브 스크린샷 확인.
- **비고**: 이 환경 playwright MCP는 browser_evaluate 1초 타임아웃으로 각 신규 구역 화면 직접 순회는 불가 → 구역 렌더·연결은 DOM 하니스로 결정적 검증.

### 12. 신규 맵 에이전트 이벤트 4종 — ✅ 완료
- **요구**: 신규 맵 각각에 에이전트를 활용한 추가 이벤트 개발.
- **작업**: 맵별 테마 이벤트 오브젝트(✦) + 전용 페르소나 에이전트.
  - 🌲 숲 **정령 나무**(대화형): 질문하면 자연의 지혜에 빗댄 신비로운 답.
  - 🏖️ 해변 **유리병 편지**(자동): 열면 표류자 편지를 매번 새로 생성.
  - ❄️ 설원 **말하는 눈사람**(대화형): 겨울 수다·농담.
  - 🕳️ 동굴 **수정 오라클**(대화형): 질문에 수수께끼 같은 시적 예언.
  - 구현: agents.js에 이벤트 에이전트 4종(기존 CLI 스트리밍 재사용), sprites.js 정령나무·유리병 스프라이트 + AGENT_META, world.js addEvent/eventAt + 4맵 배치 + ✦ 라벨·테마 스프라이트 렌더, game.js startEvent(대화형=startDialogue, 자동형=즉시 sendTask).
- **검증**: node --check OK, 6개 구역(이벤트 포함) 렌더 예외 없음, 대화형(인사→입력)·자동형(즉시 working) 흐름 정상, **WS로 실제 AI 응답 생성 확인**(유리병 편지·수정 오라클), Playwright 실브라우저 JS 에러 0.

### 13. NPC 추가 + 지형/문 UIUX 개선 + 위치 오류 수정 — ✅ 완료
- **요구**: NPC 더 추가 / 맵 지형지물·건물·문 UIUX 개선 / 위치 안맞는 오류 수정.
- **오류 수정(A)**: 세로 게이트 쌍(동·서) 이름표가 한 칸 치우치던 것 → 게이트 중심 정렬(라벨 그룹화). 북쪽 게이트 도로가 코딩 오두막을 가로지르던 것 → x29~30으로 경로 재배치, 게이트 (29,1)·(30,1)로 이동.
- **UIUX(B)**: ① **상호작용 프롬프트** — 문·게이트·NPC·이벤트·표지판 앞에 서면 위에 "Enter" 말풍선(깜빡임)으로 발견성↑. ② **그림자** — 나무·침엽수·야자수·캐릭터 아래 타원 그림자로 깊이감. ③ **게이트 발광 펄스**로 포털 강조.
- **NPC 추가(C)**: 마을 4명(촌장·꼬마·상인·길고양이) + 맵 4명(숲 약초꾼·해변 어부·설원 펭귄·동굴 광부). 캐릭터 색 구분 + 고양이/펭귄 전용 스프라이트. 말 걸면 페르소나 대화(기존 스트리밍 재사용).
- **변경 파일**: agents.js(NPC 8종), sprites.js(고양이·펭귄·그림자 + AGENT_META), world.js(NPC 배치·렌더·라벨·게이트 라벨/경로 수정·그림자·발광), game.js(상호작용 프롬프트).
- **검증**: node --check OK. DOM으로 8 NPC 배치/렌더·북쪽 게이트 이동·촌장 대화 시작 정상. WS로 NPC(촌장) 실제 AI 응답 확인. Playwright 스크린샷에 NPC·이름표·그림자·게이트 발광 확인, JS 에러 0.
- **비고**: 일부 마을 NPC가 살짝 "메타"하게 답하는 경향(원하면 in-fiction 가드 추가 가능).

### 14. 권장 UI/UX 개선 4종 — ✅ 완료
- **요구**: 권장하는 UI/UX 개선 진행.
- **작업**:
  - **타이틀 화면**: 시작 시 제목·부제·조작법·"Enter 로 시작"(걷기 캐릭터 + 그림자). scene 'TITLE' 추가, Enter/Space로 시작.
  - **구역 진입 배너**: 게이트 이동/시작 시 상단에 구역 이름(예: "🌲 숲") 페이드 인·아웃 표시(위치 확인).
  - **대화창 화자 초상화**: 말하는 NPC/에이전트/이벤트의 작은 픽셀 초상화를 대화창 왼쪽에 표시(정체성↑). UI.setPortrait + drawPortrait(스프라이트 확대), 텍스트는 오른쪽으로.
  - **라벨 정리**: NPC·이벤트 이름표는 플레이어 6타일 이내일 때만 표시(노이즈↓), 건물·게이트 이름표는 항상.
- **변경 파일**: game.js(타이틀·배너·초상화 연동), ui.js(초상화), world.js(라벨 정리), task.md.
- **검증**: node --check OK. DOM으로 타이틀→시작·타이틀 중 이동 잠금·NPC 대화+초상화 렌더·구역 배너 렌더 정상. Playwright로 타이틀 화면 스크린샷 확인(고해상도 한글), JS 에러 0.
- **비고**: 이 환경 playwright MCP는 browser_press_key/evaluate가 1초 타임아웃이라 인게임 키 구동 스크린샷은 불가 → 인게임 배너/초상화는 DOM 하니스로 검증.

### 15. 대화창 버그 수정 + NPC 배회/추가/상호작용 확대 — ✅ 완료
- **버그**: 특정 상호작용(예: 유리병) 후 오류 + 검은 대화상자가 영원히 안 사라짐.
  - **원인**: ① 작업 중 취소 등으로 대화를 닫은 뒤 서버에서 늦게 온 done/error 메시지가 `finishWorking()`을 호출해 닫힌 창을 다시 띄움(이때 game.talking=false라 Enter로 못 닫음). ② 한 프레임에서 예외가 나면 requestAnimationFrame이 안 걸려 루프 전체가 멈춤(=박스 영원히).
  - **수정**: ① handleServerMessage에서 `!game.talking`이면 chunk/done/error/agent_status 무시(늦은 메시지 차단). ② 렌더 루프를 try/catch로 감싸 한 프레임 실패해도 루프 유지.
- **NPC 배회**: 실외 NPC ~60%가 자동으로 인접 칸을 골라 부드럽게 이동(픽셀 보간), 충돌 칸 예약/해제로 겹침 방지. 대화 중인 NPC는 정지. 실내 NPC는 제외. (world.js updateNpcs/startNpcMove, game.js 연동)
- **NPC/상호작용 추가**: 음유시인·강아지(마을), 나무꾼(숲), 서퍼(해변), 눈토끼(설원), 보물사냥꾼(동굴) NPC 6 + 이벤트 2(소원의 우물·요정 버섯). 강아지/토끼 전용 스프라이트. → 실외 NPC 14명(배회 ~9명≈64%), 이벤트 6종.
- **변경 파일**: game.js(버그수정·배회 연동), world.js(배회 시스템·NPC/이벤트 배치·렌더), agents.js(페르소나 8), sprites.js(강아지·토끼 + 메타), task.md.
- **검증**: node --check OK. DOM으로 전 구역 렌더(신규 포함)·배회 NPC 이동·정지 NPC 고정·신규 6종 배치·취소후 늦은에러 무시 정상. WS로 소원의 우물 실제 응답. Playwright JS 에러 0.

### 16. NPC 흔들림 제거 + 90% 배회 + 먼 거리 이동 — ✅ 완료
- **요구**: 캐릭터가 방방 뛰지 않게 / 90% 캐릭터가 위치 변동 / 한두 칸이 아닌 먼 거리 랜덤 이동.
- **작업**:
  - **흔들림 제거**: drawNPC·고양이·펭귄·강아지·토끼의 sin 기반 위아래 bob 제거(이제 정지/이동 시 흔들리지 않음). 작업중 표시는 말풍선·강조 링으로 유지.
  - **90% 배회**: 정지였던 상인·나무꾼·어부·광부에 wander 추가, 촌장만 고정 → 실외 NPC 14명 중 13명(≈93%).
  - **먼 거리 이동**: 한 칸 랜덤이 아니라 **BFS 경로탐색**으로 ±13타일 내 먼 목적지를 골라 이어서 걸어가고, 도착하면 잠깐 쉬었다 새 목적지로. 벽/건물/문/게이트(=비통과 칸) 자동 회피, 충돌 칸 예약으로 NPC 겹침 방지. 대화 중 NPC는 정지.
- **변경 파일**: sprites.js(bob 제거), world.js(bfsPath/pickJourney/updateNpcs 재작성·wander 플래그), task.md.
- **검증**: node --check OK. DOM으로 상인이 최대 21타일 이동(먼 거리)·촌장 완전 고정·전 구역 렌더+이동 예외 없음 확인. Playwright JS 에러 0.

### 17. 상호작용 버그 2종 수정 + 전 NPC 전수 검증 — ✅ 완료
- **버그 A(딱 붙으면 Enter 무반응)**: NPC에 딱 붙으면 플레이어 스프라이트가 NPC 타일 행까지 겹쳐, "정면 한 칸"이 NPC를 지나쳐 감지 실패. → `getFrontPx`를 **발(서 있는 칸) 기준**으로 수정해 정렬과 무관하게 정면 칸을 정확히 잡도록 함.
- **버그 B(상호작용 시 대화 없이 전체 멈춤)**: 잠재적으로 `game.talking`이 true로 고착되면 플레이어가 영구히 멈춤. → ① 초상화 미지원 스프라이트(우물·버섯·강아지·토끼) 케이스 추가 ② update에 **자동복구 안전장치**(대화 상태인데 대화창이 닫혀 있으면 즉시 해제) 추가. (렌더 루프 try/catch는 기존 유지)
- **전수 검증(요청)**: 모든 실외 NPC/이벤트 **20개** 각각에 대해 "딱 붙은 상태로 Enter → 대화 열림 → 닫힘 → 이동 복구"를 자동 검증 → **20/20 통과**(예외 0, 엄격한 ctx로 음수 반경 등도 검사). 멈춤 자동복구도 동작 확인.
- **변경 파일**: player.js(getFrontPx), ui.js(초상화 4종), game.js(자동복구), task.md.
- **검증**: node --check OK, 전수 20/20 + 자동복구 PASS, Playwright JS 에러 0.

---

## 실행 방법
```bash
cd /Users/dohee/Documents/workspace/project/AgentVillage
npm install          # 최초 1회
node server.js        # http://localhost:47913
```
- 조작: WASD/방향키 이동(대각선 없음) · **Enter** 상호작용 · ESC 뒤로/취소 · T 타이핑 속도
- 문/출구/게이트에 닿으면 자동 전환(나선 효과)
- 마을 남쪽 게이트 → 「에이전트 공방」 구역 → 마스터(✎)에게 Enter → 커스텀 에이전트 추가/편집/삭제

## 비고
- 커밋/푸시는 사용자가 명시적으로 요청할 때만 수행.
- 작업 후 서버는 기본적으로 종료(현재는 플레이를 위해 실행 유지 중).

---

## 18. 마스터 NPC = 에이전트 팀 빌더 확장
- 요청: 공방 마스터에게 말 걸면 에이전트 수·서브에이전트·팀·역할 등을 직접 커스터마이징.
- **모델 선택**: 커스텀 에이전트 생성/편집 시 opus/sonnet/haiku 직접 선택(기존 sonnet 고정 해제).
- **팀(team) 개념 추가**: `{name, color, leadModel, execution, members}` — `teams.json`에 영구 저장.
  - 실행 방식 사용자 선택: **순차**(앞 결과를 다음 멤버에 전달) / **병렬**(동시 수행 후 종합), 리드가 결과 종합.
- 에디터 탭 2개(에이전트/팀), 팀 NPC를 공방에 배치 → 말 걸면 멀티에이전트 작업 실행.
- **변경 파일**: agents.js(팀 레지스트리·runTeamTask), server.js(팀 CRUD), public/editor.js(탭·모델·팀 폼), public/interior.js(팀 NPC), public/game.js(연동).
- **검증**: 서버 팀 CRUD 19/19, 에디터 로직 14/14.

## 19. 대화창 UX — 생각 중 문구 + 답변 전체 보기(리더)
- **생각 중 문구**: NPC 응답 대기(첫 청크 전) 동안 "답변을 준비하고 있어요/생각하는 중이에요/…" 회전 문구 + 점 애니메이션. 첫 청크 도착 시 실제 텍스트로 전환.
- **확장 리더**: 답변 완료(result) 후 **F** 키로 전체 답변을 스크롤 가능한 전체화면 오버레이로 표시(↑↓/PgUp·PgDn 스크롤, ESC/F 닫기). 대화창 하단에 "[F] 전체보기" 안내.
- **변경 파일**: public/ui.js, public/game.js.
- **검증**: DOM-스텁 하네스 14/14, 리뷰어(code-reviewer) HIGH 3건 반영(handleBack 가드·openReader 스트리밍 가드·maxScroll 의존 문서화).

## 20. 커스텀 에이전트로 팀 구성 + IME 2칸 이동 버그 픽스
- **팀원 = 내가 만든 커스텀 에이전트**: 팀 멤버를 자유 입력 역할 대신 **커스텀 에이전트 토글 선택**으로 변경. 멤버는 각자의 시스템프롬프트·모델로 동작, 삭제된 멤버는 자동 제외, 중복 제거.
- **IME 2칸 이동 버그**: 한글 조합 중 keydown이 (229 팬텀 + 실제)로 두 번 발생해 폼 항목이 2칸씩 이동하던 버그를 `e.isComposing || e.keyCode===229` 가드로 수정.
- **변경 파일**: agents.js(멤버=agentId 참조), public/editor.js(토글 선택·IME 가드), public/game.js(핸들러).
- **검증**: 서버 팀 8/8, 에디터 토글·IME 9/9, UX 재검증 8/8, 전체 node --check 9/9, 서버 HTTP 200, Playwright 콘솔 에러 0(타이틀/월드 렌더 확인).

## 21. GitHub 저장소 생성 및 커밋/푸시
- 사용자 요청으로 `agent-village` 저장소 생성, 작업 내용을 서브시스템별로 세분화 커밋 후 푸시.
